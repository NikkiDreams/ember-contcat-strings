import { concatStrings } from '../../../helpers/concat-strings';
import { module, test } from 'qunit';

module('Unit | Helper | concat strings');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = concatStrings(42);
  assert.ok(result);
});
